// Placeholder for the missing index.js file

/**
 * פונקציית Placeholder שנוצרה כדי לעבור את שלב ה-Build.
 */
export function createPageUrl(pageName) {
    // מניחים נתיב פשוט עבור ה-Build
    return pageName.toLowerCase();
}
